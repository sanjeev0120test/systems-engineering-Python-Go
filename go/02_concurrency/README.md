# Step 33 — Go concurrency

Goroutines, channels, and `sync.WaitGroup` — Go's answer to parallel I/O and CPU work without Python's GIL.

## Topics

- Launching goroutines with `go func()`
- Collecting results via buffered channels
- Waiting for workers with `sync.WaitGroup`

## Commands

```bash
./run.sh 33
./check.sh 33
```
