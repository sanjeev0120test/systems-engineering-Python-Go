# Step 32 — Go basics

Syntax, structs, and error handling — the building blocks of Go infrastructure tools.

## Topics

- Variables, functions, and `fmt` printing
- Structs (`User`) and methods-by-convention
- Multiple return values and explicit `error` handling

## Commands

```bash
./run.sh 32
./check.sh 32
```

Or from this directory:

```bash
go run .
go test ./...
```
