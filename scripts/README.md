# Maintenance Scripts

For repo maintainers — learners can ignore these.

| Script | Purpose |
|--------|---------|
| `validate_all.py` | Full structure + pytest + random spot checks |
| `neutralize_wording.py` | One-time wording normalization (already applied) |
| `fix_tests.py` / `fix_test_globals.py` / `patch_test_helpers.py` | Test import helpers (historical) |

Learners run: `./setup.sh`, `./run.sh N`, `./check.sh N` only.
