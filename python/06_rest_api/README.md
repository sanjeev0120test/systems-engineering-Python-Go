# REST API (Steps 11–12)

Two halves of HTTP service work: **client** then **server**.

| Step | Folder | Topic |
|------|--------|-------|
| 11 | `client/` | `requests`, retries, health polling |
| 12 | `server/` | FastAPI `/health` and `/ready` on localhost |

**Step 12** blocks the terminal (server running). Open a second terminal for `./run.sh 11` to poll the server.

Next: Step 13 → `python/07_concurrency/`
